name: aws_iot_device
description: A wrapper around MQTT to make it easier to connect to AWS IoT.
version: 0.0.2
homepage: https://bitbucket.org/TheBosZ/aws_iot_device/
author: Nathan Kerr <nathankerr@gmail.com>

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  amazon_cognito_identity_dart: ^0.0.20
  mqtt_client: ^5.2.0
  tuple: ^1.0.2

dev_dependencies:
  test: ^1.0.0
